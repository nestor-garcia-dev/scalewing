---
'@scalewing/react': minor
---

Add `SegmentedControl` `variant` (`'compact' | 'filled'`): `filled` stretches the track to its container with equal-width segments and an accent-filled selection (generated `sw-segmented-filled`). The default `compact` look is unchanged.
