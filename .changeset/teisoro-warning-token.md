---
'@scalewing/tokens': minor
---

Add the `warning` semantic colour (light `#B54708`, dark `#FDB022`): the caution between `success` and `danger`, readable on the canvas at 4.5:1 in both schemes. `--sw-color-warning` is generated with the other keys and `Text color="warning"` accepts it.
