# Native example agent rules

Follow the root `AGENTS.md`. On conflict, the root wins.

## Hard rules

1. This app demonstrates public `@scalewing/react-native` exports. It is not a source of tokens.
2. Use spacing step props, not CSS class names.
3. Do not import `@scalewing/react` or CSS stylesheets.
4. Do not add product domain screens here.
